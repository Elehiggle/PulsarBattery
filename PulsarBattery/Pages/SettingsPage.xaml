<Page
    x:Class="PulsarBattery.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="32">
            <Grid HorizontalAlignment="Stretch">
                <StackPanel Spacing="16">

                    <Grid ColumnSpacing="12" RowSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Battery check interval" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="How often the app polls the device and updates the dashboard (and can trigger alerts)." Opacity="0.7" TextWrapping="Wrap" />
                                <NumberBox Minimum="0.1"
                                           Maximum="120"
                                           SpinButtonPlacementMode="Inline"
                                           Value="{Binding PollIntervalMinutes, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Log interval" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="How often readings are added to history." Opacity="0.7" TextWrapping="Wrap" />
                                <NumberBox Minimum="0.1"
                                           Maximum="240"
                                           SpinButtonPlacementMode="Inline"
                                           Value="{Binding LogIntervalMinutes, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Notifications" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="Use this to verify Windows notifications are configured correctly." Opacity="0.7" TextWrapping="Wrap" />

                            <StackPanel Spacing="2">
                                <TextBlock x:Name="NotificationEnvText" Opacity="0.7" />
                                <TextBlock x:Name="NotificationRegText" Opacity="0.7" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="Send test notification" Click="SendTestNotification_Click" />
                                <Button Content="Refresh status" Click="RefreshNotificationStatus_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
